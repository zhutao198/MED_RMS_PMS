# R175 开发计划 — 项目管理增强（全量）

> **依据**: PRD v2.2（`prd-med-rms-v2.2-2026-07-11.md`）
> **总估算**: 30 人天（后端 15 + 前端 15）
> **并行策略**: 后端 Track + 前端 Track 同步推进

---

## Track A — 后端（按实现顺序）

| # | 任务 | 文件 | 估时 |
|---|------|------|------|
| A1 | Excel 依赖 + 导出/导入端点 | `pom.xml` + `ProjectController` + `ProjectService` | 0.5d |
| A2 | Project 加 `budget_alarm_pct` 字段 | DDL + `Project.java` | 0.3d |
| A3 | `project_activity` 表 + Entity + Service | DDL + `ProjectActivity.java` + `ProjectActivityService` | 0.5d |
| A4 | 甘特图拖拽后端支持（`adjustTask` + 重算 CPM） | `GanttService.java` + `GanttController.java` | 0.5d |
| A5 | 项目克隆 `ProjectService.cloneProject()` | `ProjectService.java` + `ProjectController.java` | 0.5d |
| A6 | 工时超预算校验 + 通知 | `WorklogService.java` + `NotificationService.java` | 0.3d |
| A7 | 活动流事件发布（各 Service 加 event publish） | 各 Service + `ProjectActivityService` | 1.0d |
| A8 | 健康度评分 `ProjectService.calculateHealthScore()` | `ProjectService.java` + `ProjectController.java` | 0.5d |
| A9 | 跨项目资源调整建议 `suggestAdjustments()` | `GanttService.java` | 0.5d |
| A10 | 合规模板 type 扩展（需求/评审/项目） | `ComplianceTemplateService.java` + Mapper | 0.3d |

## Track B — 前端（按实现顺序）

| # | 任务 | 文件 | 估时 |
|---|------|------|------|
| B1 | 甘特图拖拽（Frappe Gantt 或原生拖拽） | `GanttView.vue` | 2.0d |
| B2 | 资源热力图（网格→日历热力着色） | `ResourceManagement.vue` | 1.0d |
| B3 | 任务看板（新建 `TaskBoard.vue` + 路由） | `TaskBoard.vue` + `router.ts` + `ProjectDetail.vue` | 2.0d |
| B4 | 项目克隆按钮 | `ProjectsList.vue` | 0.5d |
| B5 | Excel 导入导出 UI | `ProjectDetail.vue` + `WorklogView.vue` | 1.0d |
| B6 | 活动流时间线组件 | `ProjectActivity.vue` + `ProjectDetail.vue` | 1.5d |
| B7 | 健康度评分卡 + 雷达图 | `Dashboard.vue`（管理视角） | 1.0d |
| B8 | 项目级审计追踪视图 | `ProjectAuditLog.vue` | 0.5d |
| B9 | 跨项目资源调整建议弹窗 | `ResourceManagement.vue` | 0.5d |
| B10 | 模板管理 Tab 扩展 | `TemplateManagement.vue` + 应用模板 | 1.0d |
| B11 | 工时超预算 UI 提示 | `WorklogView.vue` | 0.3d |

---

## 执行顺序

```
Day 1-2:  A1+A2+A3  (后端基础)  +  B1  (甘特图拖拽，最复杂)
Day 3-4:  A4+A5  (后端API)     +  B2+B3  (热力图+看板)
Day 5-6:  A6+A7  (后端服务)     +  B4+B5  (克隆+Excel)
Day 7-8:  A8+A9+A10  (后端)    +  B6+B7  (活动流+评分)
Day 9-10: 联调 + Bug 修复       +  B8+B9+B10+B11
```

## 验证

- 每个前端任务完成后运行 `npx playwright test` 确认 135/135 不 regression
- 全量完成后 PRD 验收标准逐条验证
