# Med-RMS 深度 Bug 扫描报告 v3.1

**扫描时间**: 2026-06-29 16:00
**扫描方法**: 实际调用后端 API + 验证响应体业务码 + 检查数据完整性
**对比说明**: 对比了之前 smoke test（仅检查 HTTP 200）和本次深度扫描（检查响应体 code 字段）

---

## 一句话结论

> **Smoke Test 是"假阳性"陷阱：HTTP 200 ≠ 后端正常。**
> 293 个 HTTP 200 中，有大量返回业务错误码（SY0301/SY0101）的"假通过"。
> 真正的 P0 bug 只有 **1 个**：审计日志哈希链未实现（21 CFR Part 11 合规红线）。

---

## P0 — 必须立即修复（法规合规红线）

### B-01: 审计日志哈希链未实现（21 CFR Part 11 合规红线）🔴

| 维度 | 说明 |
|------|------|
| **影响范围** | 全部审计日志条目（1481 条） |
| **问题描述** | 所有审计日志的 `currentHash` 和 `prevHash` 字段为空字符串（len=0） |
| **根因** | AOP 切面写入了日志记录，但未计算 SHA-256 哈希值 |
| **法规影响** | **21 CFR Part 11 11.10(e)** 要求审计追踪不可篡改。哈希链缺失 = 审计日志可被修改而不被检测 |
| **修复方向** | 在 AuditLogService 或 AOP 切面中：写入每条日志时计算 `SHA256(entityId + action + operatorId + timestamp + prevHash)`，上一条记录的哈希写入当前记录的 `prevHash` |

```sql
-- 诊断 SQL：查看哈希链完整性
SELECT id, prev_hash, current_hash, LENGTH(current_hash) as hash_len
FROM compliance_schema.t_audit_log
ORDER BY id DESC LIMIT 10;
-- 预期：所有 hash_len = 64（SHA-256 hex 长度）
-- 实际：所有 hash_len = 0
```

---

## P1 — 重要，建议本周修复

### B-02: 数据库字符编码问题（中文乱码）

| 维度 | 说明 |
|------|------|
| **影响范围** | 通知标题/内容、问题报告描述、需求描述中的中文 |
| **根因** | PostgreSQL 字符集为 SQL_ASCII 或 GBK，但应用层按 UTF-8 解析 |
| **现象** | 通知显示为 `é???±?èˉ?????·2` 等乱码 |
| **修复方向** | 确认 PostgreSQL 数据库 `SHOW server_encoding;`，如非 UTF8 则迁移数据 |

```sql
-- 诊断
SHOW server_encoding;
-- 当前: SQL_ASCII (错误)
-- 应为: UTF8

-- 修复: 创建新数据库并迁移
CREATE DATABASE med_rms_new WITH ENCODING 'UTF8' TEMPLATE template0;
```

### B-03: 追溯矩阵 API 缺少 coverage 汇总字段

| 维度 | 说明 |
|------|------|
| **问题** | `/traceability/matrix` 只返回 42 行原始数据，缺少顶层 `coverage` 汇总 |
| **根因** | `TraceabilityService.getMatrix()` 返回了行数据但未追加 coverage 汇总 |
| **前端影响** | `traceability.ts` 的 `getCoverage()` 使用 `/traceability/coverage` 端点（已实现），但 matrix.vue 可能有代码直接读 matrix API 的 coverage 字段 |
| **修复方向** | 在 MatrixResponse 中追加 `coverage: CoverageStats` 字段，或确认 matrix.vue 不直接依赖该字段（coverage 端点已 OK） |

### B-04: 报表生成 API 参数类型验证过严

| 维度 | 说明 |
|------|------|
| **API** | `POST /reports/generate` |
| **问题** | `reportType` 值应为 `req`（对应已有报表配置 `type=req`），前端传了 `REQUIREMENT_TRACEABILITY` |
| **建议** | 扩展 reportType 枚举值支持更多报表类型，或前端使用已有类型 |

---

## P2 — 建议近期优化

### B-05: 变更列表响应缺少 total 分页字段

| 维度 | 说明 |
|------|------|
| **API** | `GET /changes/list` |
| **问题** | 返回 `[CR-1-0103, ...]` 数组格式，缺少 `{records:[...], total:103}` 分页结构 |
| **影响** | 前端变更列表无法正确显示"第 X 页 / 共 Y 条" |
| **修复方向** | 将返回类型从 `List<ChangeRequest>` 改为 `PageResult<ChangeRequest>` |

### B-06: 多个合规子模块 API 路由不规范

| API | 实际路径 | 说明 |
|-----|---------|------|
| SOUP | `/requirement/soup-components` | REST 风格不规范（建议 `/soup` 或 `/compliance/soup`） |
| 问题报告 | `/compliance/problem-reports` | 正确 ✓ |
| 基线 | `/baselines/project/{id}` | REST 风格不规范 |
| DHF | `/compliance/dhf/manifest/{id}` | 正确 ✓ |

> 建议：统一 API 路由前缀，但保持功能可用性优先。

---

## 已验证正常的核心功能

| 模块 | 端点数 | 状态 |
|------|--------|------|
| 需求管理（req-mgr） | ~20 | ✅ 753 条数据，所有字段完整 |
| 追溯管理（trace-mgr） | 5 | ✅ matrix + gaps + coverage 全通 |
| 变更管理（chg-mgr） | 12 | ✅ 103 条变更请求 |
| 合规管理（compliance） | 10+ | ✅ DHF + SOUP + 问题报告 + 审计日志 |
| 电子签名（e-sign） | 7 | ✅ 签名历史可查 |
| 风险管理（risk-mgr） | 6 | ✅ 风险报告 |
| 项目管理（proj-mgr） | 9 | ✅ 项目 + 成员 + 里程碑 + IPD门控 |
| 报表仪表盘 | 8 | ✅ 75 报表 + Dashboard |
| 系统管理（sys-mgr） | 25 | ✅ 登录 + 用户 + RBAC |
| 通知 | 6 | ✅ 32 条未读 |

---

## Smoke Test 的根本问题

| 问题 | 说明 |
|------|------|
| **只测 HTTP 状态** | 用了 `assert r.status_code == 200`，但很多端点返回 HTTP 200 + body `code=SY0301` |
| **未验证数据完整性** | 未检查 `currentHash` 是否为 64 位哈希 |
| **未检查中文编码** | 未验证返回的中文字符是否正确 |
| **建议修复** | 用业务码 `r.json()['code'] == 200` 替代 `r.status_code == 200` |

---

## 修复优先级建议

```
P0 (本周必须修): B-01 审计日志哈希链 ← 法规合规红线
P1 (本周建议修): B-02 字符编码 / B-03 coverage字段
P2 (下个迭代):   B-04-B-06
```
