### R259 后续变更记录 — 实施完成

**实施时间**: 2026-07-29 19:16
**commit**: <待填>
**tag**: R259（annotated）

| 子项 | 状态 | 关键改动 |
|------|------|---------|
| R259.1 诊断 | ✅ | PostgreSQL 报"嵌套聚合函数调用"（COUNT(MAX(...))）+ 历史数据 PRJ-ECG3-001 非纯数字后缀导致 CAST 失败 |
| R259 修复 | ✅ | ProjectService.generateProjectNo + ChangeService.generateChangeNo：selectCount → selectObjs + 正则过滤 `project_no ~ '^PRJ-[0-9]{6}$'` |
| R259.2 验证 | ✅ | UI/API 创建英文项目成功（PRJ-000004 R258-E2E-Test 可见）|

**验证**：
- mvn test med-rms-project → **98/98 PASS**
- UI 创建英文项目：成功，PRJ-000004
- UI 项目列表：4 个（修复前 3 个）
- 解析：selectObjs 正确返回 List<Object>，第一行第一列即 MAX 值

**回滚**：`git checkout R259`

---

