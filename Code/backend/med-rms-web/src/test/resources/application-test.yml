spring:
  application:
    name: med-rms-test
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  security:
    filter:
      order: -1

server:
  port: 0

mybatis-plus:
  mapper-locations:
  type-aliases-package:

logging:
  level:
    root: WARN
    com.zhutao.medrms: DEBUG