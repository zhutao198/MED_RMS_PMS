# Med-RMS 需求流转流程梳理（依据 PRD v2.2）

> 来源：PRD `prd-med-rms-v2.2-2026-07-11.md`
> 梳理日期：2026-08-12
> 范围：需求池 → 四层需求（URS→PRS→SRS→DRS）→ 需求→项目任务转化 → 实现/验证/基线/变更

---

## 一、总体架构

Med-RMS 需求流转分为两条主线：

1. **需求池（临时暂存区）**：非结构化原始诉求进入正式需求体系的入口。
2. **四层正式需求层级**：URS → PRS → SRS → DRS 逐层拆解评审，最后通过「需求→任务转化」落地到项目执行层。

整个链路可概括为五段式：

```
需求收集 → 四层拆解评审 → 任务执行 → 实现验证 → 基线/变更
 (需求池)   (URS→PRS→SRS→DRS)   (任务)   (DRS已实现/已验证)  (已基线/已变更)
```

---

## 二、需求池（PRD §6 / §7.1 需求池状态机，352-378）

需求收集池（RequirementPool）是正式需求之前的**临时暂存区**，拥有独立状态机：

```
PENDING ──解析──▶ PARSED ──转换为URS──▶ CONVERTED（生成正式 URS）
   │                  │
   └──── 拒绝 ────────┴──▶ REJECTED（保留记录，禁止物理删除已转换条目）
```

| 操作 | 源→目标 | 说明 |
|------|--------|------|
| 手动添加/批量导入 | → PENDING | 新建/批量导入 |
| 解析 | PENDING→PARSED | 系统或人工解析原始描述 |
| 转换为 URS | PENDING/PARSED→CONVERTED | `POST /{id}/convert` 生成正式 URS |
| 拒绝 | PENDING/PARSED→REJECTED | 填拒绝理由，保留记录 |
| 删除 | PENDING/REJECTED→物理删除 | 已转换条目禁止删除 |

---

## 三、四层需求层级架构（PRD §7.1.1，388-405）

```
L1 URS（用户需求规格）   来源：用户/客户/市场/法规        映射 DCP1 概念阶段
L2 PRS（产品需求规格）   来源：URS 拆解 + 产品定义         映射 DCP2 计划阶段
L3 SRS（系统需求规格）   来源：PRS 拆解 + 系统架构约束      映射 DCP3 开发阶段
L4 DRS（设计需求规格）   来源：SRS 拆解 + 设计约束         关联测试用例/代码模块
```

每一层向下逐层 1:N 转化，转化时**自动继承上游法规关联与追溯关系**。

---

## 四、各层级状态机（PRD §7.1.2-7.1.5）

### URS 基础状态机（所有层通用骨架，441-443 行）

```
草稿 → 评审中 → 已通过 → 已基线化 → 已变更 → 评审中（变更循环）
              ↘ 已驳回 → 草稿（修改后重新提交）
已基线化 → 已废弃（仅研发总监）
```

| 状态 | 含义 | 可执行操作 |
|------|------|-----------|
| 草稿 | 新建或驳回后修改中 | 编辑、提交评审、删除 |
| 评审中 | 已提交等待评审 | 查看评论、撤回 |
| 已通过 | 评审通过 | 创建基线、发起变更 |
| 已驳回 | 评审未通过 | 编辑、重新提交 |
| 已基线化 | 已纳入基线快照 | 发起变更、废弃 |
| 已变更 | 基线化后发生变更 | 重新评审 |
| 已废弃 | 不再有效 | 仅查看 |

### PRS（483-492 行）：URS 基础 + 「待拆解 / 已拆解」

```
已通过 → 待拆解 → 已拆解
```

| 状态 | 含义 | 可执行操作 |
|------|------|-----------|
| 待拆解 | 评审通过，等待拆解为 SRS | 在拆解工作台创建 SRS |
| 已拆解 | 至少 1 条 SRS 已创建并关联 | 查看拆解结果 |

### SRS（522-534 行）：URS 基础 + 待拆解/已拆解 + 待验证/已验证

```
已通过 → 待拆解 → 已拆解
已通过 → 待验证 → 已验证
```

| 状态 | 含义 | 可执行操作 |
|------|------|-----------|
| 待拆解 | 评审通过，等待拆解为 DRS | 在拆解工作台创建 DRS |
| 已拆解 | 至少 1 条 DRS 已创建并关联 | 查看拆解结果 |
| 待验证 | DRS 实现完成，等待测试验证 | 关联测试用例 |
| 已验证 | 所有关联测试用例通过 | 关闭 |

### DRS（536 行起）：同 SRS 骨架，是编码实现直接输入

DRS 实现/验证状态机（§7.1.9，570-580 行）：

```
已基线化 → 实现中 → 已实现 → 待验证 → 已验证 → 已关闭
          ↘ 已变更 → 评审中（循环）
```

| 状态 | 含义 | 可执行操作 |
|------|------|-----------|
| 实现中 | 研发工程师正在编码 | 填报工时 |
| 已实现 | 编码完成 | 提交验证 |
| 待验证 | 等待测试工程师验证 | 关联测试用例 |
| 已验证 | 测试通过 | 关闭 |
| 已关闭 | 需求已满足 | 仅查看 |

> **重要**：PRD 中**不存在**「已提交(Submitted)」「评审通过(ReviewApproved)」这两个中间态——「已提交等待评审」即「评审中」；「评审通过」即「已通过」。

---

## 五、层间转化规则（PRD §7.1.6，584-598）

| 转化 | 基数 | 触发条件 | 自动执行 | 校验规则 |
|------|------|---------|---------|---------|
| URS→PRS | 1:N | URS「已通过」 | 继承法规关联 + 建立来源追溯 | 每条 URS ≥1 条 PRS；PRS 验收标准覆盖 URS |
| PRS→SRS | 1:N | PRS「已通过」 | 继承法规关联 + 接口需求传递 | 每条 PRS ≥1 条 SRS；SRS 验收标准覆盖 PRS |
| SRS→DRS | 1:N | SRS「已通过」 | 继承法规关联 + 接口规格 + 测试策略 | 每条 SRS ≥1 条 DRS；DRS 验收标准覆盖 SRS |

支持 N:1 反向汇总（多条上游汇总为 1 条下游）。

**转化完整性校验（拆解工作台内置）**：
1. 覆盖率校验：下游验收标准并集是否覆盖上游全部验收标准
2. 法规一致性校验：下游法规关联是否包含上游全部法规关联
3. 完整性校验：所有下游需求必填字段是否均已填写

---

## 六、拆解工作台（PRD §7.2.5，752-766）

驱动层间转化的核心工作界面：

1. **树形视图**：左侧项目需求层级树，支持展开/折叠
2. **拆解面板**：右侧选中需求详情 + 拆解操作区
3. **拆解操作**：选上游 →「创建下游需求」→ 自动继承上游关联 → 填下游字段 → 保存
4. **覆盖率校验**：拆解后自动校验下游验收标准是否覆盖上游
5. **看板视图**（P1 增强）：拖拽卡片式并行拆解

**验收要点**：所有上游验收标准被覆盖后，**上游需求状态自动更新为「已拆解」**。

**合规强约束**：拆解转化操作需触发**电子签名**（21 CFR Part 11 §11.50），继承的法规关联不得遗漏（IEC 62304 Clause 5.2.2）。

---

## 七、需求 → 项目任务转化（PRD §7.7.1，FR-1.10 / FR-2.6，1165-1209）

### 转化路径

```
SRS / DRS → 1:N 拆解 → 任务
```

> 只有**系统需求层（SRS）和设计需求层（DRS）**可向下拆解为项目任务，URS/PRS 不直接生成任务。

### 触发方式（v1.51 内联工作台）

- 在「需求→任务」页面，选中待转化需求 → 点「转化为任务」→ 当前行下方**内联展开工作台**（非弹窗）
- 工作台含：需求详情、任务草稿列表（可编辑标题/起止日期/工时/优先级）、重新生成草稿/添加任务/保存/取消

### 任务字段

任务ID(TSK-{项目编号}-{序号})、标题、详细描述、上游需求(SRS/DRS-ID)、负责人、优先级(紧急/高/中/低)、预估/实际工时、起止日期、依赖任务、状态(待开始/进行中/已完成/已阻塞)。

### 任务状态机

`待开始 → 进行中 → 已完成`（异常分支：已阻塞）

### 任务状态 ↔ 需求状态双向联动规则（1194-1201 行）

| 任务状态变化 | 需求状态影响 |
|------------|------------|
| 所有任务「已完成」 | DRS 状态自动变为**「已实现」**（并通知测试工程师） |
| 任一任务「已阻塞」超 3 天 | 通知项目经理，DRS 标记「风险」 |
| 任务优先级「紧急」 | 对应需求优先级建议提升 |
| 实际工时 > 预估×150% | 通知项目经理，标记「工时超支」 |

---

## 八、完整端到端流程（含任务与验证闭环）

```
[需求池]  PENDING→PARSED→CONVERTED
                              │ 生成
                              ▼
[URS]  草稿→(提交评审)→评审中→(评审通过)→已通过
                              │ 拆解工作台「拆解为 PRS」
                              ▼
[PRS]  草稿→评审中→已通过→待拆解→已拆解（上游 URS 自动标记已拆解）
                              │ 拆解为 SRS
                              ▼
[SRS]  草稿→评审中→已通过→待拆解→已拆解
                              │ 拆解为 DRS / 或拆解为任务
                              ▼
[DRS]  草稿→评审中→已通过→待拆解→已拆解
                              │ 拆解为任务（§7.7.1 需求→任务转化）
                              ▼
[任务] 待开始→进行中→已完成
                              │ 全部任务「已完成」
                              ▼
[DRS] 自动更新为「已实现」（1198 行联动规则）
                              │ DRS 实现完成
                              ▼
[DRS] 已验证（测试工程师验证，关联测试用例，覆盖率统计）、通知测试
                       └─(验收不通过)─▶ 待验证（重新验证循环）
                              │
                              ▼
[URS 基线化] 已通过→已基线化→实现中→已实现→待验证→已验证→已关闭
                              │
                              └─(发起变更)─▶ 已变更→评审中（变更循环，下游自动标 suspect）
```

---

## 九、关键联动与合规约束

1. **任务→DRS 自动回流（1198/1205 行）**：最后一条关联任务完成时，DRS 自动变「已实现」并通知测试工程师——需求体系与项目执行体系的**闭合点**。
2. **风险/超支监控（1199/1201 行）**：任务阻塞超 3 天、工时超支 150% 自动通知项目经理并标记 DRS 风险/超支。
3. **电子签名（IEC 62304 §5.8）**：DRS「已实现」标记需审计日志（94-95 行合规验收），关联测试用例覆盖需满足 §5.7 追溯性。
4. **变更 suspect 标记（FR-0.10）**：上游需求变更后，所有下游关联项自动标记「suspect」（黄色警告），负责人确认或修改后清除。
5. **IPD 阶段门衔接（§7.1.7）**：

| 阶段门 | 需求层级行为 | 系统校验 |
|--------|------------|---------|
| DCP1 概念 | URS 创建与评审 | 所有 URS「已通过」；每条 URS ≥1 法规条款 |
| DCP2 计划 | URS→PRS，PRS 评审 | 所有 URS 已拆解为 PRS；PRS 全「已通过」；追溯完整率≥90% |
| DCP3 开发 | PRS→SRS→DRS，实现 | PRS/SRS 全已拆解；DRS 实现完成率≥80% |
| DCP4 验证 | 测试验证 | DRS 验证通过率≥95%；测试覆盖率 100%；追溯闭环率≥95% |
| DCP5 发布 | 全链路确认 | 追溯闭环率≥99%；合规证据包可生成；无高风险未闭环项 |

---

## 十、实现对齐备注（2026-08-12 已修复项）

- **草稿→提交评审→评审中→评审通过**：原实现单 reviewer 模式提交后直接跳到 `REVIEW_APPROVED`（跳过中间态），不符合 PRD。已修复为：提交后停留 `InReview`（评审中），评审工作台「通过并签名」后进入 `ReviewApproved`（已通过），与 PRD「草稿→评审中→已通过」三态主链路对齐。

---

## 十一、PRD vs 实现 差异清单（代码核对，2026-08-12）

> 依据后端 `RequirementStatus.java`、`RequirementPoolService.java`、`RequirementService.java`、`RequirementTaskService.java` 实际代码核对。

### 11.1 状态机规模差异

| 维度 | PRD（§7.1.2-7.1.5） | 实现（`RequirementStatus.java`） |
|------|--------------------|-------------------------------|
| 需求状态总数 | ~7 基础态 + 逐层扩展（待拆解/已拆解/待验证/已验证/实现中/已实现/已关闭） | **18 态**：Draft/Submitted/InReview/ReviewApproved/ReviewRejected/PendingVerify/Implemented/Approved/Rejected/InProgress/InTest/Verified/Baseline/Decomposed/Suspect/Withdrawn/Closed/Retired + 兼容态 PendingDecompose |

### 11.2 状态命名 / 语义差异（关键）

| PRD 状态 | 实现状态 | 一致性 |
|---------|---------|--------|
| 草稿 | Draft | ✓ |
| 评审中（已提交等待评审） | InReview | ✓ |
| 已通过 | ReviewApproved（评审通过）+ Approved（审批通过）两态 | ⚠ 实现拆成两步 |
| 已驳回 | Rejected / ReviewRejected | ⚠ 实现两态 |
| 已基线化 | Baseline | ✓ |
| **已变更** | **无独立态**（用 Suspect 标记影响） | ✗ PRD 有独立「已变更→评审中」循环态，实现缺 |
| 已废弃 | Retired（退役） | ⚠ 命名不同但语义近 |
| 待拆解 | PendingDecompose | ⚠ 命名不同 |
| 已拆解 | Decomposed | ✓ |
| 待验证 | PendingVerify（等待验证准入） | ⚠ 语义不完全一致 |
| 实现中 | InProgress | ✓ |
| 已实现 | Implemented | ✓ |
| 已验证 | Verified | ✓ |
| 已关闭 | Closed | ✓ |
| （无） | Submitted / InTest / Withdrawn / Suspect / Retired / PendingDecompose | ⚠ 实现扩展态，PRD 未定义 |

### 11.3 流程节点差异（重要）

| # | 流程环节 | PRD 要求 | 实现行为 | 差异 |
|---|---------|---------|---------|------|
| 1 | **提交评审入口** | 草稿 → 直接提交评审 | ~~`submitForReview` 要求 `DECOMPOSED` 才可提交~~ **已修复（2026-08-12）**：当前 `submitForReview`（RequirementService 387-390 行）允许 `DRAFT` 或 `DECOMPOSED` 提交，草稿可直接提交评审，与 PRD 对齐。**（本章原描述已过时，与第十章自述一致，特此更正）** | ✓ 已修复（原 ✗ 为误判） |
| 2 | **评审通过→已通过** | 评审通过即「已通过」 | 实现拆为 `InReview → ReviewApproved(评审通过) → Approved(审批通过)` 两步，且有 FR-0.17 操作序列强制检查（须先评审通过才能审批） | ⚠ 有意扩展（产品增强），符合「草稿→评审中→已通过」主链路，可接受 |
| 3 | **任务回流 DRS** | 所有任务「已完成」→ DRS 自动变「已实现」并通知测试 | `RequirementTaskService.syncRequirementStatus`：全部任务 `DONE` → 需求置 `Implemented`（非 InTest）；并调 `notifyTesterAndQaOnImplemented` 通知测试工程师 + 抄送质量工程师 | ✓ 已修复（2026-08-12） |
| 3b | **任务回流通知测试工程师+质量工程师** | DRS 已实现需通知测试工程师安排验证（PRD §7.7.1 验收标准 1205 行） | `notifyTesterAndQaOnImplemented`：主送 `TESTER`、抄送 `QA`（角色编码与前端 `ProjectMembersAdd.vue` 下拉一致）；项目未配置则回退通知项目经理并标注缺失角色（方案 A 兜底） | ✓ 已补齐（2026-08-12） |
| 4 | **任务阻塞联动** | 任一任务「已阻塞」超 3 天 → 通知 PM + DRS 标「风险」 | `updateTaskStatus` 进入 `BLOCKED` 记录 `blockedAt`；超 3 天（`BLOCK_OVERDUE_DAYS=3`）→ `notifyPmBlockOverdue` + 置 `Suspect` 态并**独立标记 `isRisk`**（与 `isSuspect` 分离，H1 方案 A） | ✓ 已修复（2026-08-12）+ H1 语义拆分已落地 |
| 5 | **需求池 PARSED** | `PENDING → PARSED → CONVERTED` 三态流转 | 新增 `parsePoolItem`（PENDING→PARSED，`RequirementPoolController POST /{id}/parse`）；`convertToUrs` 放宽 PENDING/PARSED 可转换；`rejectPoolItem`/`deletePoolItem` 允许 PARSED | ✓ 已修复（2026-08-12） |
| 6 | **工时超支联动** | 实际工时 > 预估×150% → 通知 PM + 标「工时超支」 | `updateTaskActualHours` 中 `actualHours > estimatedHours × 1.5`（OVERTIME_RATIO，用户确认用 150% 而非项目 budgetAlarmPct）→ `notifyPmOvertime` + 置 `Suspect` 态并**独立标记 `isOvertime`**（与 `isSuspect` 分离，H1 方案 A） | ✓ 已修复（2026-08-12）+ H1 语义拆分已落地 |

### 11.4 任务状态机差异

| PRD 任务状态 | 实现任务状态（`RequirementTaskService`） | 差异 |
|-------------|----------------------------------------|------|
| 待开始 | TODO | ⚠ 命名不同 |
| 进行中 | IN_PROGRESS | ✓ |
| 已完成 | DONE | ⚠ 命名不同 |
| 已阻塞 | BLOCKED | ✓ |
| （无） | IN_TEST / CANCELLED | ⚠ 实现扩展态 |

### 11.5 已实现一致的部分（验证通过）

- 需求池 `PENDING → CONVERTED` 生成 URS（Draft）✓
- 拆解 `decomposeRequirement`：父级自动置 `DECOMPOSED`（已拆解）✓（RequirementService 356 行）
- 层间转化 `URS→PRS→SRS→DRS` 链路与基数校验存在 ✓
- 评审多 reviewer 收口：`castReviewVote` 全部投完 → `REVIEW_APPROVED`/`REVIEW_REJECTED` ✓（RequirementService 499-510 行）
- 变更 suspect 标记：`APPROVED/IN_PROGRESS/IN_TEST → SUSPECT` 追溯变更触发 ✓（RequirementStatus 22 行）

### 11.6 建议后续处理（2026-08-12 更新）

> 差异清单 11.3-#1~#6 及 #3b 已在 2026-08-12 修复轮次全部闭合（见各条 ✓ 标注）。以下为评审新发现的待确认项，详见 `CODE_REVIEW_REQUIREMENT_FLOW_2026-08-12.md`；其中 H1/H2 已于 2026-08-12 经产品/合规确认口径并完成落地。

1. **【已修复·文档回写】#1**：原描述"要求 DECOMPOSED 才可提交"已过时，当前允许 `DRAFT` 提交，本章 11.3-#1 已更正为 ✓（误判）。
2. **【已修复·#3/#3b/#4/#5/#6】**：均已实现并通过 `RequirementTaskServiceTest` 验证。
3. **【P2·合规 H2·已豁免 C】DRS「已实现」审计缺口**：`syncRequirementStatus` 自动状态回流直接 `updateById`，未走 `@AuditLog`。经合规确认，该回流为**系统非人工决策**（依据任务完成态自动推导需求态），不触发人工决策/签名，`@AuditLog` 适用性豁免；状态变更已落库并可追溯（保留 before/after 可通过后续定时快照），合规可接受。前端看板 + 状态机日志已可佐证，无需补审计日志。
4. **【P3·语义 H1·已落地 A】`Suspect` 过载**：#4（阻塞风险）、#6（工时超支）、11.2（变更嫌疑）原共用 `isSuspect`+`Suspect` 态。已采用 **方案 A（最小改动）**：保留 `isSuspect` 用于变更/任务阻塞导致的 Suspect 语义；新增独立布尔 **`isRisk`**（#4 阻塞超期）与 **`isOvertime`**（#6 工时超支），看板分别展示「⚠ 阻塞超期」「⚠ 工时超支」标签。DDL：`232_risk_overtime_flags.sql`。
5. **【P4·可选 H4】需求级工时聚合**：#6 为单任务超支判断（符合 PRD 单任务语义），如需"需求整体工时超支"监控可补聚合统计。
6. **【保留】#2 评审拆两步**：有意产品扩展，FR-0.17 序列校验兜底，不改。
7. **【保留】11.2 已变更态**：用 `Suspect` 表达"受影响"为已知取舍，独立「已变更→评审中」闭环未实现，不改。

