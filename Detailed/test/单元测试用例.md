# 单元测试用例 - Med-RMS 系统

> 文档版本：v1.0 | 编制日期：2026-05-28 | 基线：详细设计 v1.1 + 数据字典

---

## 目录

1. [需求管理模块（req-mgr）](#1-需求管理模块req-mgr)
2. [追溯管理模块（trace-mgr）](#2-追溯管理模块trace-mgr)
3. [变更管理模块（chg-mgr）](#3-变更管理模块chg-mgr)
4. [合规管理模块（compliance）](#4-合规管理模块compliance)
5. [电子签名模块（e-sign）](#5-电子签名模块e-sign)

---

## 1. 需求管理模块（req-mgr）

### 1.1 需求创建测试

#### TC-REQ-001: 创建URS需求 - 必填字段校验

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-001 |
| **用例标题** | 创建URS需求 - 必填字段校验 |
| **测试模块** | RequirementService.createRequirement() |
| **测试类型** | 单元测试 - 边界值测试 |
| **优先级** | P0 |
| **前置条件** | 用户已登录，项目存在 |
| **测试数据** | level=URS, title="ECG Signal Acquisition", priority=MUST, projectCode="PRJ001" |
| **测试步骤** | 1. 调用createRequirement()，title=null<br>2. 调用createRequirement()，priority=null |
| **预期结果** | 抛出IllegalArgumentException，提示"需求标题不能为空"/"优先级不能为空" |

#### TC-REQ-002: 创建URS需求 - CTI子表数据一致性校验

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-002 |
| **用例标题** | 创建URS需求 - CTI子表数据一致性校验 |
| **测试模块** | RequirementService.validateLevelCtiConsistency() |
| **测试类型** | 单元测试 - 跨字段校验 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试数据** | level=URS, ursData=null |
| **测试步骤** | 调用createRequirement()，level=URS但ursData=null |
| **预期结果** | 抛出BusinessException，错误码XF-001，提示"URS层级必须提供ursData" |

#### TC-REQ-003: 创建PRS需求 - 层级校验

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-003 |
| **用例标题** | 创建PRS需求 - 层级校验 |
| **测试模块** | RequirementService.validateLevelCtiConsistency() |
| **测试类型** | 单元测试 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试数据** | level=PRS, prsData包含productFunction, performanceIndex |
| **测试步骤** | 调用createRequirement()，level=PRS且prsData完整 |
| **预期结果** | 创建成功，返回RequirementDetailVO，requirementType=PRS |

#### TC-REQ-004: 需求编号自动生成

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-004 |
| **用例标题** | 需求编号自动生成 |
| **测试模块** | RequirementNumberGenerator.generate() |
| **测试类型** | 单元测试 |
| **优先级** | P1 |
| **前置条件** | 项目PRJ001下无URS需求 |
| **测试数据** | level=URS, projectCode="PRJ001" |
| **测试步骤** | 调用generate(URS, "PRJ001") |
| **预期结果** | 返回编号格式`URS-PRJ001-0001`，序号从0001开始 |

#### TC-REQ-005: 需求编号唯一性

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-005 |
| **用例标题** | 需求编号唯一性 |
| **测试模块** | RequirementNumberGenerator |
| **测试类型** | 单元测试 - 异常场景 |
| **优先级** | P0 |
| **前置条件** | 项目PRJ001下已存在URS-PRJ001-0001 |
| **测试数据** | level=URS, projectCode="PRJ001" |
| **测试步骤** | 并发调用generate(URS, "PRJ001")两次 |
| **预期结果** | 第二次调用抛出DuplicateReqNumberException或返回URS-PRJ001-0002 |

### 1.2 状态机转换测试

#### TC-REQ-006: DRAFT→SUBMITTED状态转换

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-006 |
| **用例标题** | DRAFT→SUBMITTED状态转换 - 合法转换 |
| **测试模块** | RequirementService.changeStatus() |
| **测试类型** | 单元测试 - 状态机 |
| **优先级** | P0 |
| **前置条件** | 需求状态为DRAFT |
| **测试数据** | requirement.status=DRAFT, targetStatus=SUBMITTED |
| **测试步骤** | 调用changeStatus(id, {targetStatus: SUBMITTED}) |
| **预期结果** | 状态变更为SUBMITTED，发布RequirementStatusChanged事件 |

#### TC-REQ-007: DRAFT→APPROVED非法转换

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-007 |
| **用例标题** | DRAFT→APPROVED非法转换 |
| **测试模块** | StateMachineValidator.validateTransition() |
| **测试类型** | 单元测试 - 异常场景 |
| **优先级** | P0 |
| **前置条件** | 需求状态为DRAFT |
| **测试数据** | requirement.status=DRAFT, targetStatus=APPROVED |
| **测试步骤** | 调用changeStatus(id, {targetStatus: APPROVED}) |
| **预期结果** | 抛出IllegalStatusTransitionException，提示"DRAFT状态不允许直接转为APPROVED" |

#### TC-REQ-008: APPROVED→PENDING_DECOMPOSE转换 - DRS层级拒绝

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-008 |
| **用例标题** | APPROVED→PENDING_DECOMPOSE转换 - DRS层级拒绝 |
| **测试模块** | DcpGateValidator.validateDecompose() |
| **测试类型** | 单元测试 - 业务规则 |
| **优先级** | P0 |
| **前置条件** | DRS层级需求状态为APPROVED |
| **测试数据** | level=DRS, status=APPROVED, targetStatus=PENDING_DECOMPOSE |
| **测试步骤** | 调用changeStatus(id, {targetStatus: PENDING_DECOMPOSE}) |
| **预期结果** | 抛出DcpGateValidationException，提示"DRS层级需求不可拆解" |

#### TC-REQ-009: DRAFT→CLOSED状态转换

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-009 |
| **用例标题** | DRAFT→CLOSED状态转换 |
| **测试模块** | RequirementService.changeStatus() |
| **测试类型** | 单元测试 - 状态机 |
| **优先级** | P1 |
| **前置条件** | 需求状态为DRAFT |
| **测试数据** | requirement.status=DRAFT, targetStatus=CLOSED |
| **测试步骤** | 调用changeStatus(id, {targetStatus: CLOSED}) |
| **预期结果** | 状态变更为CLOSED |

### 1.3 需求评审测试

#### TC-REQ-010: 提交评审 - 电子签名验证

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-010 |
| **用例标题** | 提交评审 - 电子签名验证失败 |
| **测试模块** | ReviewService.createReview() |
| **测试类型** | 单元测试 - 异常场景 |
| **优先级** | P0 |
| **前置条件** | 需求状态为IN_REVIEW |
| **测试数据** | eSignToken=invalid_token |
| **测试步骤** | 调用createReview()，传入无效电子签名token |
| **预期结果** | 抛出ESignVerificationException，提示"签名验证失败" |

#### TC-REQ-011: 评审通过 - 状态转换

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-011 |
| **用例标题** | 评审通过 - 状态自动转为APPROVED |
| **测试模块** | ReviewService.advanceStatusAfterReview() |
| **测试类型** | 单元测试 - 状态机 |
| **优先级** | P0 |
| **前置条件** | 本轮所有评审人已提交，全部APPROVE |
| **测试数据** | 2个评审人，全部提交APPROVE |
| **测试步骤** | 调用isReviewComplete()，全部返回true后调用advanceStatusAfterReview() |
| **预期结果** | 需求状态变更为APPROVED，发布ReviewCompleted事件 |

#### TC-REQ-012: 评审拒绝 - 状态转换

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-012 |
| **用例标题** | 评审拒绝 - 状态自动转为REJECTED |
| **测试模块** | ReviewService.advanceStatusAfterReview() |
| **测试类型** | 单元测试 - 状态机 |
| **优先级** | P0 |
| **前置条件** | 本轮评审人已提交，存在REJECT |
| **测试数据** | 2个评审人，1个APPROVE，1个REJECT |
| **测试步骤** | 调用advanceStatusAfterReview() |
| **预期结果** | 需求状态变更为REJECTED |

#### TC-REQ-013: 同一评审人同一轮次重复评审

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-013 |
| **用例标题** | 同一评审人同一轮次重复评审 |
| **测试模块** | ReviewService.createReview() |
| **测试类型** | 单元测试 - 业务规则 |
| **优先级** | P1 |
| **前置条件** | reviewerId=1已在本轮提交评审 |
| **测试数据** | requirementId=1, reviewerId=1, round=1 |
| **测试步骤** | 同一评审人再次调用createReview() |
| **预期结果** | 抛出DuplicateReviewException，提示"同一评审人同一轮次不可重复评审" |

### 1.4 需求拆解测试

#### TC-REQ-014: 父子层级兼容性校验 - URS→PRS

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-014 |
| **用例标题** | 父子层级兼容性校验 - URS→PRS合法 |
| **测试模块** | RequirementService.validateLevelCompatibility() |
| **测试类型** | 单元测试 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试数据** | parentLevel=URS, childLevel=PRS |
| **测试步骤** | 调用validateLevelCompatibility(URS, PRS) |
| **预期结果** | 校验通过，不抛出异常 |

#### TC-REQ-015: 父子层级兼容性校验 - URS→SRS非法

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-015 |
| **用例标题** | 父子层级兼容性校验 - URS→SRS非法 |
| **测试模块** | RequirementService.validateLevelCompatibility() |
| **测试类型** | 单元测试 - 异常场景 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试数据** | parentLevel=URS, childLevel=SRS |
| **测试步骤** | 调用validateLevelCompatibility(URS, SRS) |
| **预期结果** | 抛出InvalidLevelCompatibilityException |

### 1.5 闭包表测试

#### TC-REQ-016: 添加自引用行

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-016 |
| **用例标题** | 添加自引用行 |
| **测试模块** | RequirementAncestorService.addSelfReference() |
| **测试类型** | 单元测试 |
| **优先级** | P0 |
| **前置条件** | 需求ID=1001 |
| **测试数据** | requirementId=1001 |
| **测试步骤** | 调用addSelfReference(1001) |
| **预期结果** | 创建闭包表记录(descendantId=1001, ancestorId=1001, depth=0) |

#### TC-REQ-017: 父子关联 - 闭包表维护

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-017 |
| **用例标题** | 父子关联 - 闭包表维护 |
| **测试模块** | RequirementAncestorService.linkChild() |
| **测试类型** | 单元测试 |
| **优先级** | P0 |
| **前置条件** | 父需求ID=1001(URS)，子需求ID=2001(PRS) |
| **测试数据** | parentId=1001, childId=2001 |
| **测试步骤** | 调用linkChild(1001, 2001) |
| **预期结果** | 创建闭包表记录：<br>- (2001, 1001, 1) 直接父子<br>- (2001, 2001, 0) 自引用 |

#### TC-REQ-018: 无环校验 - 检测循环依赖

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-018 |
| **用例标题** | 无环校验 - 检测循环依赖 |
| **测试模块** | RequirementAncestorService.hasCycle() |
| **测试类型** | 单元测试 - 异常场景 |
| **优先级** | P0 |
| **前置条件** | 已存在追溯：A→B，B→C |
| **测试数据** | ancestorId=A, descendantId=C |
| **测试步骤** | 调用hasCycle(A, C)，C已是A的子孙 |
| **预期结果** | 返回true，拒绝创建会形成环的链接 |

#### TC-REQ-019: 解除关联 - 闭包表清理

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-019 |
| **用例标题** | 解除关联 - 闭包表清理 |
| **测试模块** | RequirementAncestorService.unlinkChild() |
| **测试类型** | 单元测试 |
| **优先级** | P1 |
| **前置条件** | 父子已关联 |
| **测试数据** | parentId=1001, childId=2001 |
| **测试步骤** | 调用unlinkChild(1001, 2001) |
| **预期结果** | 删除相关闭包表记录 |

### 1.6 版本管理测试

#### TC-REQ-020: 创建新版本 - 版本号递增

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-020 |
| **用例标题** | 创建新版本 - 版本号递增 |
| **测试模块** | RequirementVersionService.createVersion() |
| **测试类型** | 单元测试 |
| **优先级** | P0 |
| **前置条件** | 当前版本V1 |
| **测试数据** | requirementId=1001, 当前版本号=1 |
| **测试步骤** | 调用createVersion(1001, versionDTO) |
| **预期结果** | 创建V2，旧版本isLatest=false，新版本isLatest=true |

#### TC-REQ-021: 查询版本列表

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-REQ-021 |
| **用例标题** | 查询版本列表 |
| **测试模块** | RequirementVersionService.listVersions() |
| **测试类型** | 单元测试 |
| **优先级** | P1 |
| **前置条件** | 需求有3个版本 |
| **测试数据** | requirementId=1001 |
| **测试步骤** | 调用listVersions(1001) |
| **预期结果** | 返回3个版本，按versionNumber倒序 |

---

## 2. 追溯管理模块（trace-mgr）

### 2.1 追溯链接创建测试

#### TC-TRACE-001: 创建追溯链接 - 必填字段校验

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-TRACE-001 |
| **用例标题** | 创建追溯链接 - 必填字段校验 |
| **测试模块** | TraceLinkService.createTraceLink() |
| **测试类型** | 单元测试 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试数据** | sourceReqId=null |
| **测试步骤** | 调用createTraceLink()，sourceReqId=null |
| **预期结果** | 抛出IllegalArgumentException，提示"源需求ID不能为空" |

#### TC-TRACE-002: 创建追溯链接 - 自引用拒绝

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-TRACE-002 |
| **用例标题** | 创建追溯链接 - 自引用拒绝 |
| **测试模块** | TraceLinkService.createTraceLink() |
| **测试类型** | 单元测试 - 异常场景 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试数据** | sourceReqId=1001, targetReqId=1001 |
| **测试步骤** | 调用createTraceLink()，sourceReqId=targetReqId |
| **预期结果** | 抛出BusinessException，错误码020201，提示"不能创建自引用追溯" |

#### TC-TRACE-003: 层级方向校验 - URS→PRS合法

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-TRACE-003 |
| **用例标题** | 层级方向校验 - URS→PRS合法 |
| **测试模块** | TraceLinkService.validateTraceDirection() |
| **测试类型** | 单元测试 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试数据** | sourceLevel=URS, targetLevel=PRS, traceType=satisfies |
| **测试步骤** | 调用validateTraceDirection(URS, PRS, satisfies) |
| **预期结果** | 校验通过 |

#### TC-TRACE-004: 层级方向校验 - URS→SRS非法

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-TRACE-004 |
| **用例标题** | 层级方向校验 - URS→SRS非法 |
| **测试模块** | TraceLinkService.validateTraceDirection() |
| **测试类型** | 单元测试 - 异常场景 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试数据** | sourceLevel=URS, targetLevel=SRS, traceType=satisfies |
| **测试步骤** | 调用validateTraceDirection(URS, SRS, satisfies) |
| **预期结果** | 抛出BusinessException，错误码020101，提示"追溯类型无效"或层级不匹配 |

#### TC-TRACE-005: 循环检测 - 拒绝形成环的追溯

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-TRACE-005 |
| **用例标题** | 循环检测 - 拒绝形成环的追溯 |
| **测试模块** | TraceLinkService.validateNoCycle() |
| **测试类型** | 单元测试 - 异常场景 |
| **优先级** | P0 |
| **前置条件** | 已存在追溯链：A→B→C |
| **测试数据** | sourceId=A, targetId=C（A已是C的上游） |
| **测试步骤** | 调用createTraceLink(A, C) |
| **预期结果** | 抛出BusinessException，错误码020203，提示"闭环检测：追溯链接会形成环" |

### 2.2 追溯覆盖率计算测试

#### TC-TRACE-006: 追溯覆盖率计算 - 空项目

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-TRACE-006 |
| **用例标题** | 追溯覆盖率计算 - 空项目 |
| **测试模块** | TraceMatrixCalculator.calculateCoverage() |
| **测试类型** | 单元测试 |
| **优先级** | P1 |
| **前置条件** | 项目无需求 |
| **测试数据** | projectId=1, requirements=[] |
| **测试步骤** | 调用calculateCoverage(1) |
| **预期结果** | overallRate=0, totalRequirements=0, tracedRequirements=0 |

#### TC-TRACE-007: 追溯覆盖率计算 - 部分追溯

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-TRACE-007 |
| **用例标题** | 追溯覆盖率计算 - 部分追溯 |
| **测试模块** | TraceMatrixCalculator.calculateCoverage() |
| **测试类型** | 单元测试 |
| **优先级** | P1 |
| **前置条件** | 项目有5个需求，其中3个有下游追溯 |
| **测试数据** | total=5, traced=3 |
| **测试步骤** | 调用calculateCoverage(1) |
| **预期结果** | overallRate=60%, totalRequirements=5, tracedRequirements=3 |

#### TC-TRACE-008: DCP阈值校验 - DCP2覆盖率

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-TRACE-008 |
| **用例标题** | DCP阈值校验 - DCP2覆盖率 |
| **测试模块** | TraceMatrixCalculator |
| **测试类型** | 单元测试 - 业务规则 |
| **优先级** | P1 |
| **前置条件** | 项目处于DCP2阶段 |
| **测试数据** | coverage=75% |
| **测试步骤** | 计算覆盖率后检查是否满足DCP2≥80%阈值 |
| **预期结果** | 低于阈值，触发预警或拒绝基线 |

### 2.3 反向链接自动创建测试

#### TC-TRACE-009: 创建正向链接 - 自动创建反向链接

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-TRACE-009 |
| **用例标题** | 创建正向链接 - 自动创建反向链接 |
| **测试模块** | TraceLinkService.createTraceLink() |
| **测试类型** | 单元测试 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试数据** | sourceReqId=1, targetReqId=2, traceType=satisfies |
| **测试步骤** | 调用createTraceLink()，traceType=satisfies |
| **预期结果** | 创建两条记录：<br>1. (1→2, satisfies)<br>2. (2→1, satisfied_by) |

---

## 3. 变更管理模块（chg-mgr）

### 3.1 变更请求创建测试

#### TC-CHG-001: 创建变更请求 - 必填字段校验

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-CHG-001 |
| **用例标题** | 创建变更请求 - 必填字段校验 |
| **测试模块** | ChangeRequestService.createChangeRequest() |
| **测试类型** | 单元测试 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试数据** | title=null, reason=null |
| **测试步骤** | 调用createChangeRequest()，title和reason为null |
| **预期结果** | 抛出IllegalArgumentException，提示"变更标题不能为空"/"变更原因不能为空" |

#### TC-CHG-002: 创建变更请求 - CR编号自动生成

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-CHG-002 |
| **用例标题** | 创建变更请求 - CR编号自动生成 |
| **测试模块** | ChangeRequestService.createChangeRequest() |
| **测试类型** | 单元测试 |
| **优先级** | P1 |
| **前置条件** | 项目PRJ001下无变更请求 |
| **测试数据** | projectId=1, projectCode="PRJ001" |
| **测试步骤** | 调用createChangeRequest() |
| **预期结果** | crCode格式为`CR-PRJ001-0001` |

#### TC-CHG-003: 紧急变更 - 直接进入执行状态

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-CHG-003 |
| **用例标题** | 紧急变更 - 直接进入执行状态 |
| **测试模块** | ChangeRequestService.createChangeRequest() |
| **测试类型** | 单元测试 - 业务规则 |
| **优先级** | P1 |
| **前置条件** | 无 |
| **测试数据** | changeType="EMERGENCY" |
| **测试步骤** | 调用createChangeRequest()，changeType=EMERGENCY |
| **预期结果** | status="Executing"，事后补审批 |

### 3.2 变更审批测试

#### TC-CHG-004: 变更审批 - 状态校验

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-CHG-004 |
| **用例标题** | 变更审批 - 状态校验 |
| **测试模块** | ChangeRequestService.approveChange() |
| **测试类型** | 单元测试 - 异常场景 |
| **优先级** | P0 |
| **前置条件** | 变更状态为Draft（非PendingApproval） |
| **测试数据** | status="Draft", targetStatus="Approved" |
| **测试步骤** | 调用approveChange()，状态非PendingApproval |
| **预期结果** | 抛出BusinessException，错误码030201，提示"仅待审批状态可审批" |

#### TC-CHG-005: 重大变更审批 - QA经理权限校验

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-CHG-005 |
| **用例标题** | 重大变更审批 - QA经理权限校验 |
| **测试模块** | ChangeRequestService.approveChange() |
| **测试类型** | 单元测试 - 权限 |
| **优先级** | P0 |
| **前置条件** | 变更类型为MAJOR，当前用户非QA_MANAGER角色 |
| **测试数据** | changeType="MAJOR", currentUserRole="DEVELOPER" |
| **测试步骤** | 调用approveChange() |
| **预期结果** | 抛出BusinessException，错误码030202，提示"重大变更需QA经理审批" |

#### TC-CHG-006: 变更批准 - 状态更新

| 字段 | |------|
| **用例编号** | TC-CHG-006 |
| **用例标题** | 变更批准 - 状态更新 |
| **测试模块** | ChangeRequestService.approveChange() |
| **测试类型** | 单元测试 |
| **优先级** | P0 |
| **前置条件** | 变更状态为PendingApproval |
| **测试数据** | status="PendingApproval", result="APPROVED" |
| **测试步骤** | 调用approveChange()，result=APPROVED |
| **预期结果** | status变更为"Approved"，创建ChangeApproval记录 |

### 3.3 变更执行测试

#### TC-CHG-007: 变更执行 - 状态校验

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-CHG-007 |
| **用例标题** | 变更执行 - 状态校验 |
| **测试模块** | ChangeRequestService.executeChange() |
| **测试类型** | 单元测试 - 异常场景 |
| **优先级** | P0 |
| **前置条件** | 变更状态为Draft |
| **测试数据** | status="Draft" |
| **测试步骤** | 调用executeChange() |
| **预期结果** | 抛出BusinessException，提示"仅已批准变更可执行" |

#### TC-CHG-008: 变更执行 - 创建需求新版本

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-CHG-008 |
| **用例标题** | 变更执行 - 创建需求新版本 |
| **测试模块** | ChangeRequestService.executeChange() |
| **测试类型** | 单元测试 |
| **优先级** | P0 |
| **前置条件** | 变更已批准，存在已确认的影响分析 |
| **测试数据** | status="Approved", confirmedImpacts=1 |
| **测试步骤** | 调用executeChange() |
| **预期结果** | 为受影响需求创建新版本，记录ChangeExecution |

### 3.4 状态机转换测试

#### TC-CHG-009: 变更状态转换 - 合法流程

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-CHG-009 |
| **用例标题** | 变更状态转换 - 合法流程 |
| **测试模块** | ChangeRequestService |
| **测试类型** | 单元测试 - 状态机 |
| **优先级** | P1 |
| **前置条件** | 无 |
| **测试数据** | Draft→Analyzing→PendingApproval→Approved→Executing→Completed |
| **测试步骤** | 按顺序执行状态转换 |
| **预期结果** | 每步转换成功 |

#### TC-CHG-010: 变更状态转换 - 非法跳跃

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-CHG-010 |
| **用例标题** | 变更状态转换 - 非法跳跃 |
| **测试模块** | ChangeRequestService |
| **测试类型** | 单元测试 - 异常场景 |
| **优先级** | P1 |
| **前置条件** | 状态为Draft |
| **测试数据** | 尝试从Draft直接到Approved |
| **测试步骤** | 调用approveChange()，状态为Draft |
| **预期结果** | 抛出BusinessException，错误码030201 |

---

## 4. 合规管理模块（compliance）

### 4.1 审计日志测试

#### TC-COMP-001: 审计日志记录 - CREATE操作

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-COMP-001 |
| **用例标题** | 审计日志记录 - CREATE操作 |
| **测试模块** | AuditLogService.log() |
| **测试类型** | 单元测试 |
| **优先级** | P0 |
| **前置条件** | 无 |
| **测试数据** | entityType="Requirement", entityId=1, action="CREATE" |
| **测试步骤** | 调用log()记录创建操作 |
| **预期结果** | 创建AuditLog记录，包含prevHash（上一条记录的哈希） |

#### TC-COMP-002: 审计日志记录 - 哈希链校验

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-COMP-002 |
| **用例标题** | 审计日志记录 - 哈希链校验 |
| **测试模块** | AuditLogService.verifyHashChain() |
| **测试类型** | 单元测试 |
| **优先级** | P0 |
| **前置条件** | 存在多条审计日志 |
| **测试数据** | logs: [L1, L2, L3] |
| **测试步骤** | 调用verifyHashChain() |
| **预期结果** | 返回true，每条日志的hash=Hash(prevHash + content) |

#### TC-COMP-003: 审计日志导出 - CSV格式

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-COMP-003 |
| **用例标题** | 审计日志导出 - CSV格式 |
| **测试模块** | AuditLogService.generateCsv() |
| **测试类型** | 单元测试 |
| **优先级** | P2 |
| **前置条件** | 存在审计日志 |
| **测试数据** | logs含10条记录 |
| **测试步骤** | 调用generateCsv(logs) |
| **预期结果** | 返回CSV字符串，包含表头和所有记录 |

### 4.2 SOUP组件测试

#### TC-COMP-004: SOUP组件创建 - 许可证类型校验

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-COMP-004 |
| **用例标题** | SOUP组件创建 - 许可证类型校验 |
| **测试模块** | SoupComponentService |
| **测试类型** | 单元测试 |
| **优先级** | P1 |
| **前置条件** | 无 |
| **测试数据** | licenseType="GPL" |
| **测试步骤** | 创建SOUP组件，licenseType=GPL |
| **预期结果** | 创建成功，但标记为高风险（GPL具有传染性） |

#### TC-COMP-005: SOUP组件异常检测 - 许可证过期

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-COMP-005 |
| **用例标题** | SOUP组件异常检测 - 许可证过期 |
| **测试模块** | SoupComponentService.getAnomalies() |
| **测试类型** | 单元测试 - 业务规则 |
| **优先级** | P1 |
| **前置条件** | 组件许可证已过期 |
| **测试数据** | licenseExpiry=2025-01-01（早于当前日期） |
| **测试步骤** | 调用getAnomalies(componentId) |
| **预期结果** | 返回异常列表包含{LICENSE_EXPIRED, severity=HIGH} |

#### TC-COMP-006: SOUP组件异常检测 - 无异常

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-COMP-006 |
| **用例标题** | SOUP组件异常检测 - 无异常 |
| **测试模块** | SoupComponentService.getAnomalies() |
| **测试类型** | 单元测试 |
| **优先级** | P2 |
| **前置条件** | 组件许可证有效，无安全漏洞 |
| **测试数据** | licenseExpiry=2027-01-01 |
| **测试步骤** | 调用getAnomalies(componentId) |
| **预期结果** | 返回空列表 |

---

## 5. 电子签名模块（e-sign）

### 5.1 电子签名测试

#### TC-ESIGN-001: 电子签名 - 签名密码校验

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-ESIGN-001 |
| **用例标题** | 电子签名 - 签名密码校验 |
| **测试模块** | ElectronicSignatureService.verifyPassword() |
| **测试类型** | 单元测试 - 异常场景 |
| **优先级** | P0 |
| **前置条件** | 用户已设置签名密码 |
| **测试数据** | signPassword="wrong_password" |
| **测试步骤** | 调用verifyPassword(userId, "wrong_password") |
| **预期结果** | 抛出BusinessException，错误码050201，提示"签名密码错误" |

#### TC-ESIGN-002: 电子签名 - OTP验证码校验

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-ESIGN-002 |
| **用例标题** | 电子签名 - OTP验证码校验 |
| **测试类型** | 单元测试 - 异常场景 |
| **优先级** | P0 |
| **前置条件** | 用户已绑定TOTP |
| **测试数据** | otpCode="123456"（错误） |
| **测试步骤** | 调用verifyTotp(userId, "123456") |
| **预期结果** | 抛出BusinessException，错误码050202，提示"OTP验证码错误或已过期" |

#### TC-ESIGN-003: 电子签名 - 签名记录创建

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-ESIGN-003 |
| **用例标题** | 电子签名 - 签名记录创建 |
| **测试模块** | ElectronicSignatureService.sign() |
| **测试类型** | 单元测试 |
| **优先级** | P0 |
| **前置条件** | 用户密码和OTP验证通过 |
| **测试数据** | signerId=1, intent="APPROVE", documentType="Requirement", documentId=100 |
| **测试步骤** | 调用sign()，签名验证通过后 |
| **预期结果** | 创建ElectronicSignature记录，signatureHash非空，isValid=true |

#### TC-ESIGN-004: 电子签名 - 重签功能

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-ESIGN-004 |
| **用例标题** | 电子签名 - 重签功能 |
| **测试模块** | ElectronicSignatureService.reSign() |
| **测试类型** | 单元测试 |
| **优先级** | P1 |
| **前置条件** | 存在有效签名记录 |
| **测试数据** | signatureId=1, signerId=1, reason="修改审批意见" |
| **测试步骤** | 调用reSign(signatureId, signerId, reason) |
| **预期结果** | 旧签名isValid=false，新签名isValid=true，签名哈希不同 |

#### TC-ESIGN-005: 电子签名验证 - 哈希值校验

| 字段 | 值 |
|------|-----|
| **用例编号** | TC-ESIGN-005 |
| **用例标题** | 电子签名验证 - 哈希值校验 |
| **测试模块** | ElectronicSignatureService.verifySignature() |
| **测试类型** | 单元测试 |
| **优先级** | P0 |
| **前置条件** | 存在签名记录 |
| **测试数据** | signatureId=1 |
| **测试步骤** | 调用verifySignature(signatureId) |
| **预期结果** | 返回true，签名未被篡改 |

---

## 附录

### A. 错误码索引

| 错误码 | 模块 | 说明 |
|--------|------|------|
| REQ-xxx | 需求管理 | 001-099 |
| VER-xxx | 版本管理 | 100-199 |
| XF-xxx | 跨字段校验 | 520-599 |
| BIZ-xxx | 业务规则 | 600-699 |
| 020101 | 追溯类型无效 | 参数校验 |
| 020201 | 自引用追溯 | 业务规则 |
| 020202 | 追溯链接已存在 | 业务规则 |
| 020203 | 闭环检测 | 业务规则 |
| 030101 | 变更标题不能为空 | 参数校验 |
| 030201 | 状态转换不合法 | 业务规则 |
| 030202 | 重大变更需QA经理审批 | 权限 |
| 050201 | 签名密码错误 | 业务规则 |
| 050202 | OTP验证码错误 | 业务规则 |

### B. 测试数据模板

#### 需求创建DTO

```java
RequirementCreateDTO dto = new RequirementCreateDTO();
dto.setLevel(ReqLevel.URS);
dto.setProjectCode("PRJ001");
dto.setTitle("ECG Signal Acquisition");
dto.setPriority(MoscowPriority.MUST);
// URS特有数据
UrsCreateDTO ursData = new UrsCreateDTO();
ursData.setSourceRegulation("IEC 60601-2-25");
ursData.setUserScenario("心电监护仪临床使用");
ursData.setAcceptanceCriteria("采样率≥500Hz");
dto.setUrsData(ursData);
```

#### 状态变更DTO

```java
StatusChangeDTO dto = new StatusChangeDTO();
dto.setTargetStatus(ReqStatus.SUBMITTED);
dto.setChangeReason("提交评审");
```

### C. Mock对象说明

| 对象 | Mock方式 | 说明 |
|------|---------|------|
| RequirementRepository | @Mock | 使用doReturn()设置findById返回值 |
| RequirementAncestorRepository | @Mock | 使用doReturn()设置闭包表查询 |
| StateMachineValidator | @Mock | 预设状态转换校验结果 |
| ESignService | @Mock | 预设签名验证结果 |
| ApplicationEventPublisher | @Mock | 捕获发布的事件 |

---

> **文档结束** — 单元测试用例 v1.0